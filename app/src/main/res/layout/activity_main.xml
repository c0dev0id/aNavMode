<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <org.mapsforge.map.android.view.MapView
        android:id="@+id/mapView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Crosshair fixed at screen centre; not clickable so touches reach MapView -->
    <de.codevoid.aNavMode.map.CrosshairView
        android:id="@+id/crosshair"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="false"
        android:focusable="false" />

    <!-- Download card stack — top-right, cards stack downward -->
    <LinearLayout
        android:id="@+id/downloadCardContainer"
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_margin="12dp"
        android:orientation="vertical"
        android:clipChildren="false" />

    <!-- Confirm: add waypoint at crosshair position -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddWaypoint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:layout_margin="16dp"
        android:src="@android:drawable/ic_input_add"
        android:contentDescription="Add waypoint at crosshair" />

    <!-- Debug tools FAB — top left -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabDebug"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="16dp"
        android:src="@android:drawable/ic_menu_more"
        android:contentDescription="Debug menu" />

    <!-- POI category toggles — bottom-right -->
    <LinearLayout
        android:id="@+id/poiToggles"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:orientation="horizontal">

        <ToggleButton
            android:id="@+id/toggleFuel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textOn="Fuel"
            android:textOff="Fuel"
            android:checked="false"
            android:minWidth="0dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp" />

        <ToggleButton
            android:id="@+id/toggleFood"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:textOn="Food"
            android:textOff="Food"
            android:checked="false"
            android:minWidth="0dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp" />

    </LinearLayout>

    <!-- Debug panel — floating card, toggled by fabDebug, initially hidden -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/debugPanel"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_marginTop="80dp"
        android:layout_marginStart="16dp"
        android:visibility="gone"
        app:cardElevation="8dp"
        app:cardCornerRadius="8dp"
        xmlns:app="http://schemas.android.com/apk/res-auto">

        <include layout="@layout/sheet_debug" />

    </com.google.android.material.card.MaterialCardView>

</FrameLayout>
